import { PaginatorPipePipe } from './paginator-pipe.pipe';

describe('PaginatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
